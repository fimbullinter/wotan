language: node_js
node_js:
  - node
cache: yarn
script:
  - yarn compile
  - yarn lint
  - yarn check-dependencies
  - yarn test
after_success:
  - echo $NPM_AUTH > ~/.npmrc
  - npm whoami
  - node scripts/nightly "$TRAVIS_COMMIT_RANGE"
