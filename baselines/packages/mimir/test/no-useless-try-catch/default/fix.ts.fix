if (Boolean())
    {}

for (; Boolean();)
    {
        console.log('a');
        console.log('b');
    }

while (Boolean())
    {
        console.log('a');
        console.log('b');
    }

do
    {}
while (Boolean());

if (Boolean())
    console.log('a');

if (Boolean())
    {class C {}}

if (Boolean()) {
    console.log('a');
        console.log('b');
}

let foo = 1;
if (Boolean()) {
    console.log(foo);
    {
        const foo = 1;
        console.log(foo);
    }
}

if (Boolean()) {
    {
        const foo = 1; // TODO doesn't shadow a variable in the outer scope
        console.log(foo);
    }
}

label: {}
