class Pass {
    constructor(private propA: string) {}
}

class Fail {
    public propA: string;
    constructor(propA: string) {
        this.propA = propA;
        ~~~~~~~~~~~~~~~~~~~ [error parameter-properties: Use a parameter property instead of assigning to members in the constructor.]
    }
}
